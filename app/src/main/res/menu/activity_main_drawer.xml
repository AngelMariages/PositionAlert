<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_map" android:icon="@drawable/ic_action_map"
              android:title="@string/menu_map" />
        <item android:id="@+id/nav_destinationsList" android:icon="@drawable/ic_action_destinations"
            android:title="@string/menu_destinations" />
    </group>
    <group >
        <item
            android:id="@+id/activeModeItem"
            android:title="@string/active_mode"
            app:actionLayout="@layout/active_switch"
            android:icon="@drawable/ic_action_active_mode" />
    </group>

</menu>